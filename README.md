# dejavu
Find duplicate files and directories

## Prerequisites

- CMake 4.1+
- Ninja (`winget install Ninja-build.Ninja` on Windows, or `apt install ninja-build` on Linux)
- A C++20 compiler (MSVC on Windows, GCC/Clang on Linux)
